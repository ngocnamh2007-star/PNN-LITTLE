"use client";
import { useEffect, useState } from "react";
type Account = { username: string; disabled: boolean };
type Info = { intro: string; contact: string; address: string; phone: string; facebook: string; twitter: string; links: string };
export default function OwnerClient() {
  const [accounts, setAccounts] = useState<Account[]>([]);
  const [info, setInfo] = useState<Info>({ intro: "", contact: "", address: "", phone: "", facebook: "", twitter: "", links: "" });
  const [message, setMessage] = useState(""); const [ownerPassword, setOwnerPassword] = useState("");
  async function load() { const a = await fetch("/api/owner/accounts"); if (a.ok) setAccounts((await a.json()).accounts); const i = await fetch("/api/site-info"); if (i.ok) setInfo((await i.json()).info); }
  useEffect(() => { void load(); }, []);
  async function saveInfo() { const r = await fetch("/api/site-info", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify(info) }); setMessage(r.ok ? "Đã lưu footer thành công" : "Không thể lưu footer"); }
  async function logout() { await fetch("/api/owner/logout", { method: "POST" }); window.location.href = "/owner/login"; }
  async function accountAction(username: string, body: object) { await fetch("/api/owner/accounts", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ username, ...body }) }); await load(); }
  return <main className="admin-page"><header className="admin-header"><div><p className="eyebrow">PNN LITTLE</p><h1>Quản trị hệ thống</h1><p>Tổng số khách hàng: <strong>{accounts.length}</strong></p></div><button onClick={() => void logout()}>Đăng xuất</button></header><section className="admin-panel owner-accounts"><h2>Tài khoản khách hàng</h2>{accounts.map((a) => <div className="owner-account" key={a.username}><span className={a.disabled ? "status-dot offline" : "status-dot"} /><strong>{a.username}</strong><span>{a.disabled ? "Đã khóa" : "Đang sử dụng"}</span><button onClick={() => void accountAction(a.username, { disabled: !a.disabled })}>{a.disabled ? "Mở khóa" : "Khóa"}</button><button onClick={() => { const password = prompt("Mật khẩu mới (ít nhất 8 ký tự):"); if (password) void accountAction(a.username, { password }); }}>Đổi mật khẩu</button><button className="delete-account-button" onClick={async () => { if (confirm(`Xóa tài khoản ${a.username}?`)) { await fetch(`/api/owner/accounts?username=${encodeURIComponent(a.username)}`, { method: "DELETE" }); await load(); } }}>Xóa</button></div>)}</section><section className="admin-panel owner-accounts"><h2>Giới thiệu & liên hệ footer</h2><label className="admin-field"><span>Giới thiệu</span><textarea value={info.intro} onChange={(e) => setInfo({ ...info, intro: e.target.value })} /></label><label className="admin-field"><span>Liên kết nhanh (mỗi dòng một mục)</span><textarea value={info.links} onChange={(e) => setInfo({ ...info, links: e.target.value })} /></label><label className="admin-field"><span>Địa chỉ</span><input value={info.address} onChange={(e) => setInfo({ ...info, address: e.target.value })} /></label><label className="admin-field"><span>Điện thoại</span><input value={info.phone} onChange={(e) => setInfo({ ...info, phone: e.target.value })} /></label><label className="admin-field"><span>Email</span><input type="email" value={info.contact} onChange={(e) => setInfo({ ...info, contact: e.target.value })} /></label><label className="admin-field"><span>Facebook</span><input value={info.facebook} onChange={(e) => setInfo({ ...info, facebook: e.target.value })} /></label><label className="admin-field"><span>Twitter / X</span><input value={info.twitter} onChange={(e) => setInfo({ ...info, twitter: e.target.value })} /></label><button className="save-button" type="button" onClick={() => void saveInfo()}>Lưu footer</button>{message && <p className="save-status">{message}</p>}</section></main>;
}
