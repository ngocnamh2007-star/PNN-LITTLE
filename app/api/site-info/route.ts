import { isOwnerRequest } from "../../admin-auth";
import { readState, writeState } from "../state-store";
const KEY = "site-info";
const defaults = { intro: "Tạo món quà trực tuyến dành riêng cho người bạn thương.", contact: "pnn.little@example.com", address: "Việt Nam", phone: "", facebook: "", twitter: "", socialLinks: "Facebook |\nInstagram |\nTikTok |\nYouTube |", links: "Giới thiệu\nHỗ trợ\nLiên hệ", landingEyebrow: "MỘT BẤT NGỜ NHỎ, MỘT KỶ NIỆM LỚN", landingTitle: "Tạo món quà", landingAccent: "chỉ dành riêng cho người thương.", landingLead: "Biến lời yêu thương, ảnh kỷ niệm và những điều bí mật thành một trải nghiệm đáng nhớ — chỉ với vài phút chuẩn bị.", featureOneTitle: "Cá nhân hóa", featureOneText: "Thêm lời nhắn, ảnh, nhạc và kiểu chữ theo câu chuyện của riêng bạn.", featureTwoTitle: "Gửi riêng tư", featureTwoText: "Mỗi tài khoản có một đường link quà tặng riêng để gửi cho người thương.", featureThreeTitle: "Khoảnh khắc bất ngờ", featureThreeText: "Không gian chữ 3D, hiệu ứng sao và món quà bí mật chờ được khám phá." };
export async function GET() { return Response.json({ info: { ...defaults, ...(await readState<typeof defaults>(KEY) || {}) } }); }
export async function PUT(request: Request) { if (!(await isOwnerRequest(request))) return Response.json({ error: "Unauthorized" }, { status: 401 }); const info = { ...defaults, ...(await request.json()) } as typeof defaults; if (!info.intro?.trim() || !info.contact?.trim()) return Response.json({ error: "Vui lòng nhập đủ nội dung" }, { status: 400 }); const saved = Object.fromEntries(Object.keys(defaults).map((key) => [key, String(info[key as keyof typeof defaults] || "").trim()])); await writeState(KEY, saved); return Response.json({ info: saved }); }
